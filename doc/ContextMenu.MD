# ContextMenu  
---
```
// 使用
const contextmenu = [{
                     label: '右键菜单项',
                     icon: '',
                     show: true,
                     click: (ev) => {
                       console.log('右键菜单项点击事件')
                     }
                   }]
this.$contextmenu(contextmenu)
```
